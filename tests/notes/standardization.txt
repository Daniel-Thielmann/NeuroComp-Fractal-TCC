test_fractal.py → Fractal puro (Higuchi com PCA+QDA) 

test_logpower.py → LogPower puro com LDA 

test_csp_fractal.py → CSP + Fractal com LDA 

test_csp_logpower.py → CSP + LogPower com QDA 

test_fbcsp_fractal.py → FBCSP + Fractal com QDA+PCA 

test_fbcsp_logpower.py → FBCSP + LogPower com QDA 




### escrever related works
### tabela com cada linha um sujeito, e as colunas vão ser sujeito, logo depois os métodos, uma tabela com 11 linhas e 7 colunas, na pos de cada um deles colocar o kappa (tipo uma acuracia + eficiente). calcular kappa juntando resultados de evaluate e training, um kappa por pessoa.

